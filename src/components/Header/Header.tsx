import styles from "./Header.module.scss";

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Ant Design Dragger Test</h1>
    </header>
  );
};

export default Header;
